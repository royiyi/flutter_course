import 'package:flutter/material.dart';
import 'package:navi/pages/mainPage.dart';

class MyApp extends StatelessWidget { 
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: mainPage(),
    );
  }
}